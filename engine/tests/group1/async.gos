package main

import (
	"fmt"
)

func add(a int, b int, c int) {
    assert(a + b == c)
    fmt.Println(a, "+", b, "=", c)
}

func main() {
    go fmt.Println("hello world?")
    i := 1
    j := 2
    go add(i, j, 3)
    
    k := i + j
    assert(k == 3)
}