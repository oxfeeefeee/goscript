package main

import (
	//"fmt2"
	//"strings"
)

func main() {
    /*
	var b strings.Builder
	b.WriteString("3.......")
    b.WriteString("2.......")
    b.WriteString("1.......")
	b.WriteString("ignition")
	fmt2.Println(b.String())
*/
}
