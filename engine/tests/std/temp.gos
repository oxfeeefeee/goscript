package main

/*
var i = 10

func init() {
    i--
    assert(i == 9)
}

func main() {
	i += 2
    assert(i == 11)
	a := make([]int, 5)
	a[1] = 6
	a[1] *= 3
	assert(a[1] == 18)
	
	b1, b2,b3 := 1,2,3
	c := b1 + b2 + b3
	assert(c == 6)
} 
*/

func main() {
	var s1 = map[int]int{1:2, 3: 888}
    var v = s1[1]
    var v2 = s1[3]
    assert(v == 2)
    assert(v2 == 888)

}