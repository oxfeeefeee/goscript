package main

import (
    //"fmt2"
    "time"
)

func main() {
    //p := fmt2.Println

    now := time.Now()

    then := time.Date(
        2009, 11, 17, 20, 34, 58, 651387237, time.UTC)

    
    now.Sub(then)
}