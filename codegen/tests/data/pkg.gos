package main


import "math"
    

const k = i + 1

const i = 7

var b = a + k
var a = 8



func main() {
    var pi2 = math.Pi2
    var pi = math.Pi
    _ = pi
    _ = pi2
    assert(k == 8)
    assert(b == 16)
    assert(math.Plus4(b + math.V2 + math.C2) == 24)
    math.V2 += 2
    assert(math.Plus4(b + math.V2 + math.C2) == 28)
}